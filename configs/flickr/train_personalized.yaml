seed: 42
paths:
  manifest_csv: /workspace/flickr_aes_runs/splits/flickr_personalized_manifest.csv
  output_root: /workspace/flickr_aes_runs
  cache_subdir: clip_cache
  run_name: personalized_run
model:
  input_dim: 768
  user_emb_dim: 128
  hidden_dims: [1024, 256, 64]
  dropout: 0.3
training:
  batch_size: 512
  epochs: 5
  lr: 0.0003
  weight_decay: 0.0
  device: cpu
mlflow:
  experiment_name: flickr-personalized
  run_name: personalized-large
